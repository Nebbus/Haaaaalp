#include "Explosion.h"
#include "Object.h"

//static const double TIMER = 0.1;

//////////////////////////////////////////////////////////////////////////////////////////////
Explosion::Explosion(VGCVector pos, bool fren): spr_boom(VGCDisplay::openImage("boom.png", 1, 1))
{
	//stay_timer = VGCClock::openTimer(TIMER);													//start a timer
	position = pos;
	frend = fren;
	int obj_radius = 1;
	/*if (VGCClock::isExpired(stay_timer)) {
		VGCClock::reset(stay_timer);
		die();	VGCClock::closeTimer(stay_timer);

	}*/
}
//////////////////////////////////////////////////////////////////////////////////////////////
Explosion::~Explosion()
{
	VGCDisplay::closeImage(spr_boom);
	//VGCClock::closeTimer(stay_timer);

}
//////////////////////////////////////////////////////////////////////////////////////////////
void Explosion::update()
{
	

}
//////////////////////////////////////////////////////////////////////////////////////////////
void Explosion::render()
{
	VGCVector spr_index(0, 0);
	VGCAdjustment boom_adjustment = VGCAdjustment(0.5, 0.5);

	VGCDisplay::renderImage(
		spr_boom,
		spr_index,
		position,
		boom_adjustment
	);
}
//////////////////////////////////////////////////////////////////////////////////////////////
void Explosion::derender()
{
	VGCDisplay::closeImage(spr_boom);
//	VGCClock::closeTimer(stay_timer);
}
//////////////////////////////////////////////////////////////////////////////////////////////
int Explosion::get_radius() const
{
	return obj_radius;
}
//////////////////////////////////////////////////////////////////////////////////////////////
